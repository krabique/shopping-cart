<%= turbo_stream.replace "cart_container", partial: "carts/cart_container", locals: { cart: @cart } %>
<%= turbo_stream.replace "reset_button", partial: "carts/reset_button", locals: { cart: @cart } %>
